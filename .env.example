# Example environment variables for local development
# Copy this file to .env and fill in secrets as needed

# PostgreSQL connection string (default local parameters)
DATABASE_URL="postgresql://postgres:REDACTED_PASSWORD@localhost:5433/postgres?schema=public"

# API key used by the Fastify preValidation hook
API_KEY=
